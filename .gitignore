# Реальный инвентарь парка и логи — личные, не для публикации. Шаблон — inventory.yaml.example.
inventory.yaml
logs/

# Секреты — деплой-ключи, токены. Ничего подобного не должно лежать в этом репозитории вообще,
# правило на всякий случай.
secrets/
*.pem
*_deploy_key
*_deploy_key.pub
